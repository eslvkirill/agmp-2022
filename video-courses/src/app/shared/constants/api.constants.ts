const START_PAGE_URL = '/';
const BACKEND_URL = 'http://localhost:3004';

const ENDPOINT = {
  AUTH: 'auth',
  COURSES: 'courses',
  AUTHORS: 'authors',
};

export { START_PAGE_URL, BACKEND_URL, ENDPOINT };
